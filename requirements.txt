# Bot framework (with job queue support for background tasks)
python-telegram-bot[job-queue]>=21.0

# HTTP requests
requests>=2.31.0

# Environment variables
python-dotenv>=1.0.0

# Database
psycopg2-binary>=2.9.9

# PDF generation (simplified with WeasyPrint)
weasyprint>=60.0
markdown>=3.5.0
# Old PDF generator (if needed as fallback):
# reportlab>=4.0.0

# Local LLM support
llama-cpp-python>=0.2.0
huggingface-hub>=0.19.0

# RAG system dependencies (from ragBaseMaker)
sentence-transformers>=2.2.2
transformers>=4.35.0
chromadb>=0.4.0
numpy>=1.24.0
tqdm>=4.66.0
pydantic>=2.0.0
langchain>=0.1.0
langchain-text-splitters>=0.0.1
langchain-experimental>=0.0.1
langchain-core>=0.1.0
langchain-community>=0.0.1

# Translation (for models trained on English)
sentencepiece>=0.1.99  # Required for OPUS-MT models
sacremoses>=0.0.53  # Required for some translation models

# Document loaders (for RAG via LangChain)
pypdf>=3.0.0
python-docx>=1.0.0
openpyxl>=3.1.0  # For Excel files (.xlsx)
xlrd>=2.0.0  # For old Excel files (.xls)
unstructured>=0.10.0
# Additional dependencies for unstructured
python-magic>=0.4.27  # File type detection
pillow>=10.0.0  # Image processing
lxml>=4.9.0  # XML/HTML parsing

# Development tools (optional)
pyright>=1.1.0
