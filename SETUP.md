# 🚀 Инструкция по установке и запуску бота

## 📋 Содержание
1. [Клонирование репозитория](#1-клонирование-репозитория)
2. [Настройка конфигурации](#2-настройка-конфигурации)
3. [Создание виртуального окружения](#3-создание-виртуального-окружения)
4. [Настройка базы данных PostgreSQL](#4-настройка-базы-данных-postgresql)
5. [Загрузка документов в RAG](#5-загрузка-документов-в-rag)
6. [Настройка отказоустойчивости (опционально)](#6-настройка-отказоустойчивости-опционально)
7. [Запуск бота](#7-запуск-бота)

---

## 1. Клонирование репозитория

```bash
git clone https://github.com/AMaslov01/hackathon-dont-touch-the-grass-enjoyers
cd hackathon-dont-touch-the-grass-enjoyers/
```

---

## 2. Настройка конфигурации

Создайте файл `config.env` и заполните его следующими параметрами:

```bash
nano config.env
```

**Содержимое `config.env`:**

```env
# Telegram Bot Token - Get from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# AI Mode: 'local' or 'openrouter'
AI_MODE=local

# OpenRouter API Key - Get from https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key_here

# OpenRouter API URL
OPENROUTER_API_URL=https://openrouter.ai/api/v1/chat/completions

# ПРИМЕЧАНИЕ: AI_MODEL больше не используется!
# Модели управляются автоматически через систему премиум моделей (model_manager.py)
# Дефолтная модель определяется в зависимости от AI_MODE:
#   - local → llama3-finance (бесплатная локальная модель)
#   - openrouter → glm-4.5-air (бесплатная облачная модель)
# Пользователи могут переключать модели через команду /switch_model
#
# Для прямого тестирования OpenRouter API (если нужно):
# AI_MODEL=z-ai/glm-4.5-air:free

# Local LLM Configuration (only used when AI_MODE=local)
LOCAL_MODEL_PATH=
LOCAL_MODEL_THREADS=16
LOCAL_MODEL_CONTEXT=4096
LOCAL_MODEL_TEMPERATURE=0.7

# RAG Configuration
RAG_ENABLED=true
RAG_PERSIST_DIR=./rag_data
RAG_COLLECTION_NAME=financial_docs
RAG_TOP_K=3
RAG_MAX_CONTEXT=2000

# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=telegram_bot
DB_USER=postgres
DB_PASSWORD=postgres
```

---

## 3. Создание виртуального окружения

```bash
# Создать виртуальное окружение
python3 -m venv venv

# Активировать виртуальное окружение
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

---

## 4. Настройка базы данных PostgreSQL

### Вариант 1: Создание новой базы данных

```bash
# Создать базу данных
sudo -u postgres psql -c "CREATE DATABASE telegram_bot;"

# Применить схему базы данных
sudo -u postgres psql -d telegram_bot -f schema.sql
```

### Вариант 2: Очистка существующей базы данных (если нужно)

**Откройте PostgreSQL в другом окне терминала:**

```bash
# Подключиться к PostgreSQL
psql -h localhost -U postgres -d postgres
```

**В psql выполните:**

```sql
-- Удалить существующую базу данных
DROP DATABASE telegram_bot;

-- Создать новую базу данных
CREATE DATABASE telegram_bot;

-- Выйти из psql
\q
```

**Затем примените схему:**

```bash
sudo -u postgres psql -d telegram_bot -f schema.sql
```

---

## 5. Загрузка документов в RAG

```bash
# Сделать скрипт исполняемым
chmod +x ./load_documents.sh

# Запустить загрузку документов
./load_documents.sh
```

---

## 6. Настройка отказоустойчивости (опционально)

Для запуска бота в фоновом режиме с автоматическим перезапуском и уведомлениями о сбоях:

> ⚠️ **ВАЖНО:** Перед запуском настройте файлы согласно их внутреннему описанию:
> - `telegram-bot.service` - укажите правильные пути к проекту
> - `notify_bot_crash.sh` - укажите ваш Telegram Chat ID
> - `telegram-bot-crash-notify.service` - проверьте пути
> - `healthcheck.py` - настройте параметры проверки

### Шаг 1: Дать права на выполнение скриптам

```bash
chmod +x setup_crash_notifications.sh
chmod +x notify_bot_crash.sh
chmod +x healthcheck.py
```

### Шаг 2: Запустить установку

```bash
./setup_crash_notifications.sh
```

### Что произойдет:

✅ Создадутся файлы логов  
✅ Скопируются `.service` файлы в `/etc/systemd/system/`  
✅ Systemd перезагрузит конфигурацию  
✅ Включится автозапуск при загрузке сервера

### Управление сервисом:

```bash
# Запустить бота
sudo systemctl start telegram-bot

# Остановить бота
sudo systemctl stop telegram-bot

# Перезапустить бота
sudo systemctl restart telegram-bot

# Проверить статус
sudo systemctl status telegram-bot

# Посмотреть логи
sudo journalctl -u telegram-bot -f
```

---

## 7. Запуск бота

### Обычный запуск (для разработки)

```bash
# Убедитесь что виртуальное окружение активировано
source venv/bin/activate

# Запустить бота
python bot.py
```

### Запуск через systemd (для продакшена)

```bash
# Запустить бота как сервис
sudo systemctl start telegram-bot

# Проверить статус
sudo systemctl status telegram-bot
```

---

## 🎉 Готово!

Ваш бот запущен и готов к работе! 

Используйте команду `/start` в Telegram для начала работы с ботом.

---

## 📚 Дополнительная информация

- **Документация команд:** См. `/help` в боте
- **Конфигурация моделей:** `model_manager.py`
- **База данных:** `schema.sql`
- **AI клиент:** `ai_client.py`

---

## ⚠️ Troubleshooting

### Проблема: База данных не создается

**Решение:**
```bash
sudo service postgresql start
sudo -u postgres createdb telegram_bot
```

### Проблема: Не устанавливаются зависимости

**Решение:**
```bash
pip install --upgrade pip
pip install -r requirements.txt --no-cache-dir
```

### Проблема: Бот не запускается через systemd

**Решение:**
```bash
# Проверить логи
sudo journalctl -u telegram-bot -n 50

# Проверить права доступа
sudo chmod +x /path/to/your/bot.py
```

---

**Автор:** hackathon-dont-touch-the-grass-enjoyers  
**Лицензия:** MIT

