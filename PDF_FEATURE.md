# 📄 Генерация PDF финансовых планов

## Обзор

Бот теперь генерирует **профессиональные PDF документы** с финансовыми планами вместо обычных текстовых сообщений. Используется библиотека **ReportLab** для создания красиво отформатированных документов.

## Возможности

### ✨ Что включает PDF:

1. **Профессиональное форматирование**
   - Красивые заголовки и подзаголовки
   - Цветовая схема (зеленые оттенки для финансовой тематики)
   - Структурированные секции

2. **Автоматическая структуризация**
   - AI генерирует ответ в формате Markdown
   - PDF генератор парсит заголовки (#), списки и таблицы
   - Автоматическое форматирование

3. **Таблицы**
   - Финансовые прогнозы в виде таблиц
   - Стилизованные заголовки и данные
   - Чередующиеся цвета строк для читаемости

4. **Метаданные**
   - Имя пользователя
   - Дата создания
   - Информация о бизнесе

5. **Footer с disclaimer**
   - Юридическое уведомление о консультации

## Технические детали

### Структура файлов

```
├── pdf_generator.py       # Модуль генерации PDF
├── ai_client.py           # Обновлен для структурированного вывода
├── bot.py                 # Обновлен для отправки PDF
├── temp_pdfs/             # Временная папка для PDF (создается автоматически)
└── test_pdf.py            # Тестовый скрипт
```

### Процесс генерации

1. **Сбор информации** - Пользователь отвечает на 3 вопроса о бизнесе
2. **AI генерация** - AI создает структурированный план в Markdown
3. **PDF создание** - ReportLab конвертирует в PDF
4. **Отправка** - Telegram отправляет документ пользователю
5. **Cleanup** - Файл удаляется после отправки

### Формат AI ответа

AI теперь возвращает ответ в структурированном формате:

```markdown
# 1. Анализ текущей ситуации
- Сильные стороны
- Слабые стороны
- Возможности

# 2. Рекомендации по оптимизации
- Конкретные шаги
- Потенциальная экономия

# 3. Финансовый прогноз
| Месяц | Доходы | Расходы | Прибыль |
|-------|--------|---------|---------|
| 1     | 100000 | 80000   | 20000   |
```

## Использование

### Команда /finance

1. Пользователь вводит `/finance`
2. Отвечает на 3 вопроса о бизнесе
3. Бот показывает прогресс:
   - "🤖 Генерирую финансовый план с помощью AI..."
   - "📄 Создаю PDF документ..."
   - "📤 Отправляю PDF документ..."
4. Получает готовый PDF файл

### Fallback механизм

Если PDF генерация не удалась:
- Автоматически отправляется текстовая версия
- Пользователь все равно получает информацию
- Ошибка логируется для отладки

## API Reference

### `pdf_generator.py`

#### `FinancialPlanPDF.generate()`

```python
def generate(self, 
             ai_response: str, 
             business_info: Dict,
             user_name: str = None,
             output_path: str = None) -> str:
    """
    Генерирует PDF из AI ответа
    
    Args:
        ai_response: Структурированный ответ AI в Markdown
        business_info: Словарь с информацией о бизнесе
        user_name: Имя пользователя для персонализации
        output_path: Опциональный путь для сохранения
        
    Returns:
        Путь к созданному PDF файлу
    """
```

#### `FinancialPlanPDF.cleanup_old_pdfs()`

```python
def cleanup_old_pdfs(self, max_age_hours: int = 24):
    """Удаляет старые временные PDF файлы"""
```

### `ai_client.py`

#### `AIClient.generate_financial_plan()`

```python
def generate_financial_plan(self, business_info: dict) -> str:
    """
    Генерирует финансовый план на основе информации о бизнесе
    
    Args:
        business_info: Словарь с:
            - business_type: Тип бизнеса
            - financial_situation: Финансовая ситуация
            - goals: Цели и задачи
            
    Returns:
        Структурированный план в Markdown формате
    """
```

## Стилизация PDF

### Цветовая схема

- **Заголовки**: #1a472a (темно-зеленый)
- **Подзаголовки**: #2e7d32, #388e3c (зеленые оттенки)
- **Таблицы**: 
  - Заголовок: #2e7d32 (зеленый)
  - Чередующиеся строки: белый / #f5f5f5 (светло-серый)

### Шрифты

- **Заголовок**: Helvetica-Bold, 24pt
- **H1**: Helvetica-Bold, 16pt
- **H2**: Helvetica-Bold, 14pt
- **Body**: Helvetica, 11pt
- **Bullets**: Helvetica, 10pt

### Размеры страницы

- **Формат**: A4
- **Поля**: 2cm со всех сторон
- **Ширина контента**: 18cm

## Примеры использования

### Тестирование генерации PDF

```bash
# Запустить тест
python test_pdf.py

# Результат будет в temp_pdfs/
```

### Интеграция в код

```python
from pdf_generator import pdf_generator

# Генерация PDF
pdf_path = pdf_generator.generate(
    ai_response=ai_text,
    business_info={
        'business_type': 'Описание бизнеса',
        'financial_situation': 'Финансовая ситуация',
        'goals': 'Цели'
    },
    user_name="Иван Иванов"
)

# Отправка в Telegram
with open(pdf_path, 'rb') as f:
    await update.message.reply_document(
        document=f,
        filename="Финансовый_план.pdf"
    )

# Cleanup
os.remove(pdf_path)
```

## Troubleshooting

### PDF не генерируется

**Проблема**: Ошибка при создании PDF

**Решения**:
1. Проверьте, что ReportLab установлен: `pip install reportlab`
2. Проверьте права на запись в папку temp_pdfs
3. Проверьте логи бота для деталей ошибки

### Таблицы не парсятся

**Проблема**: Таблицы отображаются как текст

**Решения**:
1. AI должен использовать формат Markdown для таблиц
2. Проверьте, что в таблице есть разделитель `|---|---|`
3. Убедитесь, что минимум 2 строки (заголовок + данные)

### PDF слишком большой

**Проблема**: Файл превышает лимит Telegram (50 MB)

**Решения**:
1. Ограничьте размер AI ответа
2. Оптимизируйте изображения (если добавляете)
3. Уменьшите количество секций

## Будущие улучшения

### Возможные доработки:

1. **Графики и диаграммы**
   - Добавить визуализацию данных
   - Использовать matplotlib для графиков
   - Интеграция в PDF

2. **Брендирование**
   - Добавить логотип
   - Настраиваемая цветовая схема
   - Персонализированный header/footer

3. **Шаблоны**
   - Разные шаблоны для разных типов бизнеса
   - Пользовательские настройки

4. **Экспорт в другие форматы**
   - Word (DOCX)
   - Excel (XLSX) для таблиц
   - HTML для веб-просмотра

5. **История версий**
   - Сохранение старых версий планов
   - Сравнение изменений
   - Трекинг прогресса

## Зависимости

```txt
reportlab>=4.0.0      # Генерация PDF
pillow>=9.0.0         # Обработка изображений (зависимость ReportLab)
```

## Лицензия

Используется библиотека ReportLab под лицензией BSD.

## Контакты и поддержка

При возникновении проблем:
1. Проверьте логи бота
2. Запустите test_pdf.py для диагностики
3. Проверьте версии зависимостей

---

**Создано**: 13 ноября 2024  
**Версия**: 1.0  
**Автор**: AI Assistant  

