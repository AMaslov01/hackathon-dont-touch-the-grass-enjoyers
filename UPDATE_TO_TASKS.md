# Обновление: Добавление системы задач

## 📦 Что нового

✅ Система управления задачами  
✅ AI-рекомендации лучшего сотрудника  
✅ Свободные и назначенные задачи  
✅ История выполненных задач

## 🚀 Как обновить

### Шаг 1: Обновить структуру базы данных

Запустите бота один раз - таблица `tasks` создастся автоматически:

```bash
python bot.py
```

Или создайте таблицу вручную:

```sql
CREATE TABLE IF NOT EXISTS tasks (
    id SERIAL PRIMARY KEY,
    business_id INTEGER NOT NULL REFERENCES businesses(id) ON DELETE CASCADE,
    title VARCHAR(500) NOT NULL,
    description TEXT,
    assigned_to BIGINT REFERENCES users(user_id),
    created_by BIGINT NOT NULL REFERENCES users(user_id),
    status VARCHAR(20) NOT NULL DEFAULT 'available',
    ai_recommended_employee BIGINT REFERENCES users(user_id),
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    assigned_at TIMESTAMP,
    completed_at TIMESTAMP
);

-- Индексы
CREATE INDEX IF NOT EXISTS idx_tasks_business_id ON tasks(business_id);
CREATE INDEX IF NOT EXISTS idx_tasks_assigned_to ON tasks(assigned_to);
CREATE INDEX IF NOT EXISTS idx_tasks_status ON tasks(status);
```

### Шаг 2: Проверить работу

#### Для владельца:
```
/create_task

Тестовая задача
---
Это описание тестовой задачи
```

Должны увидеть:
- ✅ Задача создана
- 🤖 AI рекомендацию (если есть сотрудники с историей)

#### Для сотрудника:
```
/available_tasks
/my_tasks
```

## 📋 Новые команды

### Владелец:
- `/create_task` - Создать задачу (с AI)
- `/assign_task <task_id> <user_id>` - Назначить задачу
- `/all_tasks` - Все задачи бизнеса

### Сотрудник:
- `/available_tasks` - Свободные задачи
- `/take_task <id>` - Взять задачу
- `/my_tasks` - Мои задачи
- `/complete_task <id>` - Завершить

## 🔍 Проверка

```bash
# Проверить, что таблица создана
psql -d telegram_bot -c "\d tasks"

# Посмотреть задачи
psql -d telegram_bot -c "SELECT * FROM tasks;"
```

## ⚠️ Важно

- Требуется работающая таблица `businesses` и `employees`
- AI-рекомендации работают только при наличии выполненных задач
- Первые задачи создадутся без рекомендаций (нет истории)

## 📊 Статусы задач

- **available** - Свободна
- **assigned** - Назначена
- **in_progress** - В работе (будущее)
- **completed** - Выполнена

## 🎯 Примеры

### Создать задачу:
```
/create_task

Настроить сервер
---
Установить Nginx и настроить SSL сертификаты
```

### Посмотреть свободные:
```
/available_tasks
```

### Взять задачу:
```
/take_task 1
```

### Завершить:
```
/complete_task 1
```

## 📚 Полная документация

См. `TASKS_FEATURE.md` для подробной информации.

---

Готово! Система задач работает. 🎉

